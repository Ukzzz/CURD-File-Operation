<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Delete <%= filename %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="w-full max-w-md p-6 bg-white rounded-lg shadow-lg text-center">
    <h2 class="text-xl font-bold mb-4 text-red-600">Delete Confirmation</h2>
    <p class="mb-6 text-gray-700">Are you sure you want to delete <strong><%= filename %></strong>?</p>
    
    <form action="/delete/<%= filename %>" method="POST" class="flex justify-center space-x-4">
      <a href="/" class="bg-gray-300 text-gray-800 px-4 py-2 rounded hover:bg-gray-400">Cancel</a>
      <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Delete</button>
    </form>
  </div>
</body>
</html>

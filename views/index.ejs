<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-xl mx-auto p-6 bg-white rounded-lg shadow-lg">
      <div class="mb-8">
        <form action="/file" method="POST" class="space-y-4">
          <input
            type="text"
            name="title"
            placeholder="Enter the Title Here"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
          />
          <textarea
            name="details"
            placeholder="Enter the Details Here"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
          ></textarea>
          <button
            type="submit"
            class="w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition"
          >
            Create File
          </button>
        </form>
      </div>
      <div class="task">
        <% if (files && files.length > 0) { %> <% files.forEach(file => { %>
        <div class="tasks bg-gray-50 p-4 rounded shadow">
          <h2 class="text-xl font-semibold mb-2"><%= file %></h2>
          <a href="/file/<%=file%>" class="text-blue-500 hover:underline">Read More</a>
          <a href="/edit/<%= file %>" class="text-yellow-500 hover:underline ml-4">Edit</a>
          <a href="/delete/<%= file %>" class="text-red-500 hover:underline">Delete</a>


        </div>
        <% }); %> <% } else { %>
        <h3>No tasks yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
